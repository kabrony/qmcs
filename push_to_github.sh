#!/usr/bin/env bash

# 1) Stage all changes
git add --all

# 2) Commit with a simple message (modify as needed)
git commit -m "Cleaned up repo and finalized minimal setup"

# 3) Push to the main branch
git push -u origin main
